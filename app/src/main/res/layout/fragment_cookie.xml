<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/cookie_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cookies-Präferenzen"
        android:textSize="24sp"
        android:layout_marginBottom="16dp"
        />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/cookie_header">

        <TextView
            android:id="@+id/cookie_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/showCookiesPreferences"
            android:fontFamily="@font/swz721le"
            android:textSize="18sp"
            android:onClick="showCookiePreferences"
            />
    </ScrollView>

</RelativeLayout>